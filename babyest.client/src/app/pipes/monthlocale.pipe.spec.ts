import { MonthlocalePipe } from './monthlocale.pipe';

describe('MonthlocalePipe', () => {
  it('create an instance', () => {
    const pipe = new MonthlocalePipe();
    expect(pipe).toBeTruthy();
  });
});
