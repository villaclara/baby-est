namespace ApiTests.EndpointTests;

public class ParentEndpointTests
{
	[Fact]
	public void AddNewParentToKid_ReturnOk()
	{
		//// Arrange
		//var dbContext = await Seed.GetContext();
		//var newParent = new Parent() { Email = "newparent@test.com", Id = 10, PasswordHash = "newparent" };
		//dbContext.Parents.Add(newParent);
		//await dbContext.SaveChangesAsync();

		//// Act 
		//var result = await ParentEndpoints.AddNewParentToKid(1, 1, "newparent@test.com", dbContext);
		//var kid = dbContext.Kids.Where(k => k.Id == 1).FirstOrDefault();
		//bool value = kid.Parents.Any(p => p.Id == 10);
		//bool value2 = dbContext.Parents.Where(p => p.Id == 10).FirstOrDefault().Kids.Any(k => k.Id == 1);

		//// Assert
		//Assert.True(value);
		//Assert.True(value2);
		////Assert.True(result == TypedResults.Ok("Parent added to kid."));
	}
}
